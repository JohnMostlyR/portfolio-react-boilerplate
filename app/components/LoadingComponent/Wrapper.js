import styled from 'styled-components';

const Wrapper = styled.article`
  align-self: center;
  margin: 2em auto;
  position: relative;
  width: 100%;
  text-align: center;
`;

export default Wrapper;
